import { BattleHeader, BattlePageShell, LandingClient } from '@/src/components';

export default function BattleLanding() {
  return (
    <BattlePageShell>
      <BattleHeader />
      <LandingClient />
    </BattlePageShell>
  );
}
